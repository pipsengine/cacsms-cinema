'use client';import Link from 'next/link';import {usePathname} from 'next/navigation';
export function GenerationTabs({id}:{id:string}){const p=usePathname();const tabs=[['Image Generation',`/projects/${id}/generation/images`],['Video Generation',`/projects/${id}/generation/videos`],['Voice & Dialogue',`/projects/${id}/generation/voice`],['Music & SFX',`/projects/${id}/generation/audio`],['Generation Queue',`/projects/${id}/generation/queue`]];return <nav className="generation-tabs">{tabs.map(([label,href])=><Link key={href} className={p===href?'active':''} href={href}>{label}</Link>)}</nav>}
