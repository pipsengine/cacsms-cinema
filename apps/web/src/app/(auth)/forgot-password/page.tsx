'use client'; import Link from 'next/link'; import {FormEvent,useState} from 'react';
export default function Forgot(){const [sent,setSent]=useState(false); return <main className="auth-center"><div className="auth-card"><div className="brand-mark large">CC</div>{!sent?<><div className="eyebrow">ACCOUNT RECOVERY</div><h1>Reset your password</h1><p>Enter your work email. We’ll send a time-limited recovery link if an active account exists.</p><form className="auth-form" onSubmit={(e:FormEvent)=>{e.preventDefault();setSent(true)}}><label>Email address<input type="email" required placeholder="name@company.com"/></label><button className="btn primary wide">Send recovery link</button></form></>:<><div className="success-orb">✓</div><h1>Check your email</h1><p>Recovery instructions have been prepared. For security, the link expires after 30 minutes and can only be used once.</p></>}<Link className="back-link" href="/login">← Back to sign in</Link></div></main>}
