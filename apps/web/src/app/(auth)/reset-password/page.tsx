'use client'; import Link from 'next/link'; import {FormEvent,useState} from 'react';
export default function Reset(){const [done,setDone]=useState(false); return <main className="auth-center"><div className="auth-card"><div className="brand-mark large">CC</div>{!done?<><div className="eyebrow">SECURE PASSWORD RESET</div><h1>Create a new password</h1><p>Use a strong password you have not used for this account before.</p><form className="auth-form" onSubmit={(e:FormEvent)=>{e.preventDefault();setDone(true)}}><label>New password<input type="password" minLength={12} required/></label><label>Confirm password<input type="password" minLength={12} required/></label><div className="password-rules"><b>Password requirements</b><span>✓ At least 12 characters</span><span>✓ Uppercase, lowercase, number and symbol</span><span>✓ Must not match recent passwords</span></div><button className="btn primary wide">Update password</button></form></>:<><div className="success-orb">✓</div><h1>Password updated</h1><p>Your credentials have been changed. Existing sessions on other devices will be revoked.</p><Link className="btn primary wide link-btn" href="/login">Return to sign in</Link></>}</div></main>}
