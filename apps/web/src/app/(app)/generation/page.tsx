'use client';import Link from 'next/link';import {AppShell} from '@/components/app-shell';import {Status,StatCard} from '@/components/ui';import {genProject,providerSummary,genScenes,jobs} from '@/lib/module09-data';
const tone=(s:string)=>s==='APPROVED'||s==='COMPLETED'||s==='CONNECTED'?'green':s==='IN_PROGRESS'?'purple':s==='QUEUED'||s==='NEEDS_REVIEW'?'amber':s==='FAILED'?'red':'gray';
export default function GenerationDashboard(){return <AppShell eyebrow="PRODUCTION PIPELINE" title="AI Generation Studio" actions={<><button className="btn secondary">⚙ Provider routing</button><Link className="btn primary" href={`/projects/${genProject.id}/generation/images`}>Open generation workspace</Link></>}><div className="callout info"><b>Governed input:</b> Approved Scene Package v{genProject.scenePackageVersion} + Character Bible v{genProject.characterBibleVersion}. Every job retains scene, prompt, reference, provider and cost lineage.</div><div className="stat-grid"><StatCard label="Scenes in generation" value="5" detail="22 total planned" icon="▦"/><StatCard label="Jobs running" value="3" detail="2 queued · 1 failed" icon="✦"/><StatCard label="Approved assets" value="17" detail="Images, voice and audio" icon="✓"/><StatCard label="Generation spend" value="$7.16" detail="Current production run" icon="$"/></div><section className="workspace-card"><div className="card-head"><div><b>Scene production status</b><small>{genProject.code} · {genProject.title}</small></div><Status tone="purple">AI PROCESSING</Status></div><table className="enterprise-table"><thead><tr><th>Scene</th><th>Image</th><th>Video</th><th>Voice</th><th>Music/SFX</th><th>Variants</th><th>Cost</th><th/></tr></thead><tbody>{genScenes.map(s=><tr key={s.scene}><td><b>{s.scene}</b><small>{s.title}</small></td><td><Status tone={tone(s.image) as any}>{s.image.replace('_',' ')}</Status></td><td><Status tone={tone(s.video) as any}>{s.video.replace('_',' ')}</Status></td><td><Status tone={tone(s.voice) as any}>{s.voice.replace('_',' ')}</Status></td><td><Status tone={tone(s.audio) as any}>{s.audio.replace('_',' ')}</Status></td><td>{s.imageVariants} img · {s.videoVariants} vid</td><td>${s.cost.toFixed(2)}</td><td><Link href={`/projects/${genProject.id}/generation/images`}>Open →</Link></td></tr>)}</tbody></table></section><div className="two-col"><section className="workspace-card"><div className="card-head"><div><b>Provider subscriptions</b><small>Adapter health and usage</small></div></div>{providerSummary.map(p=><div className="list-row" key={p.key}><div><b>{p.name}</b><small>{p.cap} · {p.model}</small></div><div><small>{p.usage}</small><Status tone="green">{p.status}</Status></div></div>)}</section><section className="workspace-card"><div className="card-head"><div><b>Live generation queue</b><small>Highest priority jobs</small></div><Link href={`/projects/${genProject.id}/generation/queue`}>View all</Link></div>{jobs.slice(0,4).map(j=><div className="list-row" key={j.id}><div><b>{j.id} · {j.scene}</b><small>{j.cap} · {j.provider}</small></div><div><b>{j.progress}%</b><Status tone={tone(j.status) as any}>{j.status.replace('_',' ')}</Status></div></div>)}</section></div></AppShell>}
