'use client';import {useParams} from 'next/navigation';import {AppShell} from '@/components/app-shell';import {GenerationTabs} from '@/components/generation-tabs';import {Status} from '@/components/ui';import {genProject,genScenes} from '@/lib/module09-data';
export default function Audio(){const {id}=useParams<{id:string}>();return <AppShell eyebrow="PROJECT / AI GENERATION" title="Music & SFX" actions={<><button className="btn secondary">Apply production audio rules</button><button className="btn primary">✦ Generate cues</button></>}><div className="script-project-head"><div><span className="project-code">{genProject.code}</span><h2>{genProject.title}</h2><p>Generate or assign scene-level music and sound effects while preserving dialogue headroom and copyright controls.</p></div><Status tone="amber">4 / 5 READY</Status></div><GenerationTabs id={id}/><section className="workspace-card"><table className="enterprise-table"><thead><tr><th>Scene</th><th>Music cue</th><th>SFX cue</th><th>Dialogue ducking</th><th>Copyright</th><th>Status</th><th/></tr></thead><tbody>{genScenes.map((s,i)=><tr key={s.scene}><td><b>{s.scene}</b><small>{s.title}</small></td><td>{i<2?'Minimal curiosity pulse':i===3?'Dialogue bed — music ducked':'Light documentary rhythm'}</td><td>{i===0?'Room tone · phone pickup':i===2?'Traffic · footsteps':'Soft digital transition'}</td><td>{i===3?'−12 dB':'−6 dB'}</td><td><Status tone="green">CLEARED</Status></td><td><Status tone={s.audio==='APPROVED'||s.audio==='COMPLETED'?'green':s.audio==='QUEUED'?'amber':'gray'}>{s.audio}</Status></td><td><button className="btn secondary tiny">Open</button></td></tr>)}</tbody></table></section></AppShell>}
