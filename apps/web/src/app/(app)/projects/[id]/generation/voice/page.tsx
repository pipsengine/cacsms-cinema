'use client';import {useParams} from 'next/navigation';import {AppShell} from '@/components/app-shell';import {GenerationTabs} from '@/components/generation-tabs';import {Status} from '@/components/ui';import {genProject,voices} from '@/lib/module09-data';
export default function Voice(){const {id}=useParams<{id:string}>();return <AppShell eyebrow="PROJECT / AI GENERATION" title="Voice & Dialogue" actions={<><button className="btn secondary">Import script lines</button><button className="btn primary">✦ Generate selected audio</button></>}><div className="script-project-head"><div><span className="project-code">{genProject.code}</span><h2>{genProject.title}</h2><p>Scene dialogue and voiceover are inherited from the approved Scene Matrix; voice identities remain locked across regenerations.</p></div><Status tone="blue">VOICE LOCKS ACTIVE</Status></div><GenerationTabs id={id}/><section className="workspace-card"><div className="card-head"><div><b>Voice identity registry</b><small>Persistent voice assignments for this production</small></div></div><table className="enterprise-table"><thead><tr><th>Speaker</th><th>Voice</th><th>Scenes</th><th>Speed</th><th>Direction</th><th>Status</th></tr></thead><tbody>{voices.map(v=><tr key={v.speaker}><td><b>{v.speaker}</b></td><td>{v.voice}</td><td>{v.scenes}</td><td>{v.speed}</td><td>{v.emotion}</td><td><Status tone="green">{v.status}</Status></td></tr>)}</tbody></table></section><div className="two-col"><section className="workspace-card"><div className="card-head"><div><b>SC-004 dialogue</b><small>Fully acted scene · no narrator</small></div><Status tone="purple">GENERATING</Status></div><div className="matrix-fields"><label className="wide"><span>TUNDE</span><textarea readOnly value="Morning. You beat the traffic today."/></label><label className="wide"><span>AMARA</span><textarea readOnly value="Barely. My map changed the route twice."/></label><label><span>Emotion</span><input value="Natural, friendly office conversation" readOnly/></label><label><span>Lip-sync target</span><input value="Scene video dialogue track" readOnly/></label></div></section><section className="workspace-card"><div className="card-head"><div><b>Audio QA</b><small>Pre-approval checks</small></div></div>{['Voice identity consistency','Pronunciation','Pacing against scene duration','Clipping / peak level','Dialogue separation','Lip-sync readiness'].map((x,i)=><div className="list-row" key={x}><b>{x}</b><Status tone={i===3?'amber':'green'}>{i===3?'REVIEW':'PASS'}</Status></div>)}</section></div></AppShell>}
