'use client';import {useParams} from 'next/navigation';import {useState} from 'react';import {AppShell} from '@/components/app-shell';import {GenerationTabs} from '@/components/generation-tabs';import {Status} from '@/components/ui';import {genProject,imageVariants} from '@/lib/module09-data';
export default function Images(){const {id}=useParams<{id:string}>();const [scene,setScene]=useState('SC-003');const [selected,setSelected]=useState('IMG-302');return <AppShell eyebrow="PROJECT / AI GENERATION" title="Image Generation" actions={<><button className="btn secondary">Batch generate remaining</button><button className="btn primary">✦ Generate variants</button></>}><div className="script-project-head"><div><span className="project-code">{genProject.code}</span><h2>{genProject.title}</h2><p>Reference-locked image generation. Approved outputs become the visual input for Video Generation.</p></div><Status tone="purple">AI PROCESSING</Status></div><GenerationTabs id={id}/><div className="scene-workspace"><aside className="scene-list workspace-card"><div className="card-head"><div><b>Scenes</b><small>Choose generation target</small></div></div>{['SC-001','SC-002','SC-003','SC-004','SC-005'].map((s,i)=><button key={s} className={scene===s?'active':''} onClick={()=>setScene(s)}><span className="scene-no">{String(i+1).padStart(2,'0')}</span><div><b>{s}</b><small>{i<2?'Approved':i===2?'Needs review':'Pending generation'}</small></div></button>)}</aside><main className="scene-detail"><section className="workspace-card"><div className="card-head"><div><b>{scene} · Image job configuration</b><small>Inputs inherited from approved production blueprint</small></div><Status tone="blue">PROMPT v3</Status></div><div className="spec-grid four-col"><p><span>Provider</span><b>Image Agent Subscription</b></p><p><span>Aspect ratio</span><b>16:9</b></p><p><span>Resolution</span><b>3840 × 2160</b></p><p><span>Variants</span><b>4</b></p></div><div className="matrix-fields"><label className="wide prompt-blue"><span>IMAGE PROMPT</span><textarea defaultValue="Photorealistic Lagos morning commute, same locked Amara identity and Workday Base wardrobe, contemporary urban street, cinematic natural light, realistic traffic and pedestrians, no readable brand text."/></label><label className="wide prompt-red"><span>NEGATIVE PROMPT</span><textarea defaultValue="different face, different hairstyle, wardrobe change, wrong skin tone, logos, fake text, distorted hands, duplicated pedestrians, cartoon rendering"/></label><label className="wide"><span>LOCKED REFERENCES</span><input readOnly value="CHR-001 Amara · WRD-A1 Workday Base · LOC-003 City Commute · Scene Package v1"/></label></div></section><section className="workspace-card"><div className="card-head"><div><b>Generated variants</b><small>Select one master or request regeneration</small></div></div><div className="asset-grid">{imageVariants.map(a=><button key={a.id} className={selected===a.id?'asset-card selected':'asset-card'} onClick={()=>setSelected(a.id)}><div className="asset-placeholder">AI IMAGE<br/><small>{a.id}</small></div><div className="asset-meta"><b>Variant {a.v}</b><Status tone={a.status==='REJECTED'?'red':'amber'}>{a.status}</Status><small>Continuity score {a.score}% · {a.provider}</small><p>{a.reason}</p></div></button>)}</div><div className="button-row"><button className="btn secondary">Regenerate selected</button><button className="btn secondary">Reject</button><button className="btn primary">Approve as scene master</button></div></section></main></div></AppShell>}
