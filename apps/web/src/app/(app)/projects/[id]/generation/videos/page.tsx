'use client';import {useParams} from 'next/navigation';import {AppShell} from '@/components/app-shell';import {GenerationTabs} from '@/components/generation-tabs';import {Status} from '@/components/ui';import {genProject,genScenes} from '@/lib/module09-data';
export default function Videos(){const {id}=useParams<{id:string}>();return <AppShell eyebrow="PROJECT / AI GENERATION" title="Video Generation" actions={<><button className="btn secondary">Run continuity check</button><button className="btn primary">✦ Generate selected scene</button></>}><div className="script-project-head"><div><span className="project-code">{genProject.code}</span><h2>{genProject.title}</h2><p>Approved scene image + animation prompt + locked character/location references become provider inputs.</p></div><Status tone="purple">2 ACTIVE JOBS</Status></div><GenerationTabs id={id}/><div className="two-col"><section className="workspace-card"><div className="card-head"><div><b>SC-002 · Invisible Ranking</b><small>Video generation specification</small></div><Status tone="purple">IN PROGRESS 63%</Status></div><div className="asset-placeholder video">VIDEO PREVIEW<br/><small>Approved image master → video provider job</small></div><div className="matrix-fields"><label className="wide prompt-purple"><span>ANIMATION PROMPT</span><textarea defaultValue="Gentle controlled slider right, natural thumb scrolls once, abstract recommendation cards reorder smoothly, preserve locked face, hair, wardrobe and apartment lighting; cinematic micro-movements; no camera jump."/></label><label><span>DURATION</span><input readOnly value="8 seconds × continuation segments"/></label><label><span>OUTPUT</span><input readOnly value="4K · 24fps · 16:9"/></label><label className="wide"><span>REFERENCE CHAIN</span><input readOnly value="SC-002 Image Master v2 · CHR-001 locked · WRD-A1 · LOC-001"/></label></div><div className="button-row"><button className="btn secondary">Cancel job</button><button className="btn secondary">Switch fallback provider</button><button className="btn primary">Approve completed master</button></div></section><section className="workspace-card"><div className="card-head"><div><b>Scene video readiness</b><small>Image masters gate downstream video jobs</small></div></div>{genScenes.map(s=><div className="list-row" key={s.scene}><div><b>{s.scene} · {s.title}</b><small>Image: {s.image.replace('_',' ')}</small></div><Status tone={s.video==='COMPLETED'?'green':s.video==='IN_PROGRESS'?'purple':s.video==='QUEUED'?'amber':'gray'}>{s.video.replace('_',' ')}</Status></div>)}</section></div></AppShell>}
