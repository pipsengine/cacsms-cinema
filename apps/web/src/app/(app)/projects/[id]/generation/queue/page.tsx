'use client';import {useParams} from 'next/navigation';import {AppShell} from '@/components/app-shell';import {GenerationTabs} from '@/components/generation-tabs';import {Status} from '@/components/ui';import {genProject,jobs,providerSummary} from '@/lib/module09-data';
const tone=(s:string)=>s==='IN_PROGRESS'?'purple':s==='QUEUED'?'amber':s==='FAILED'?'red':'green';
export default function Queue(){const {id}=useParams<{id:string}>();return <AppShell eyebrow="PROJECT / AI GENERATION" title="Generation Queue" actions={<><button className="btn secondary">Pause queue</button><button className="btn primary">＋ New generation job</button></>}><div className="script-project-head"><div><span className="project-code">{genProject.code}</span><h2>{genProject.title}</h2><p>Observable provider queue with attempts, fallback routing, credit usage and retry control.</p></div><Status tone="purple">3 RUNNING</Status></div><GenerationTabs id={id}/><section className="workspace-card"><div className="card-head"><div><b>Generation jobs</b><small>Every job retains upstream lineage and prompt version</small></div></div><table className="enterprise-table"><thead><tr><th>Job</th><th>Scene</th><th>Capability</th><th>Provider / Model</th><th>Status</th><th>Progress</th><th>Attempt</th><th>Cost</th><th>Action</th></tr></thead><tbody>{jobs.map(j=><tr key={j.id}><td><b>{j.id}</b><small>Queued {j.queued}</small></td><td>{j.scene}</td><td>{j.cap}</td><td>{j.provider}<small>{j.model}</small></td><td><Status tone={tone(j.status) as any}>{j.status.replace('_',' ')}</Status></td><td><div className="progress"><i style={{width:`${j.progress}%`}}/></div><small>{j.progress}%</small></td><td>{j.attempt} / 3</td><td>${j.cost.toFixed(2)}</td><td><button className="btn secondary tiny">{j.status==='FAILED'?'Retry':'Inspect'}</button></td></tr>)}</tbody></table></section><section className="workspace-card"><div className="card-head"><div><b>Provider routing & fallback policy</b><small>Subscription adapters are replaceable without changing scene data</small></div></div><table className="enterprise-table"><thead><tr><th>Capability</th><th>Primary adapter</th><th>Reference support</th><th>Batch</th><th>Health</th><th>Usage</th><th>Status</th></tr></thead><tbody>{providerSummary.map(p=><tr key={p.key}><td><b>{p.cap}</b></td><td>{p.name}<small>{p.model}</small></td><td>{p.refs?'Yes':'No'}</td><td>{p.batch?'Yes':'No'}</td><td>{p.health}%</td><td>{p.usage}</td><td><Status tone="green">CONNECTED</Status></td></tr>)}</tbody></table></section></AppShell>}
