'use client';import {useParams} from 'next/navigation';import {AppShell} from '@/components/app-shell';import {EditingTabs} from '@/components/editing-tabs';import {Status} from '@/components/ui';import {editProject,masterVersions} from '@/lib/module10-data';
const tone=(s:string)=>s==='APPROVED'?'green':s==='IN_REVIEW'?'blue':s==='RETURNED'?'amber':'gray';
export default function Versions(){const {id}=useParams<{id:string}>();return <AppShell eyebrow={`${editProject.code} · GOVERNANCE`} title="Master Versions & Handoff" actions={<button className="btn primary">Create new master version</button>}><EditingTabs projectId={id as string} active="versions"/><div className="version-governance-grid"><section className="workspace-card"><div className="card-head"><div><b>Master version register</b><small>Immutable review and approval history</small></div></div><table className="enterprise-table"><thead><tr><th>Version</th><th>Status</th><th>Duration</th><th>Resolution</th><th>Created</th><th>Change summary</th></tr></thead><tbody>{masterVersions.map(v=><tr key={v.version}><td><b>{v.version}</b></td><td><Status tone={tone(v.status) as any}>{v.status.replace('_',' ')}</Status></td><td>{v.duration}</td><td>{v.resolution}</td><td>{v.created}</td><td>{v.summary}</td></tr>)}</tbody></table></section><aside><section className="workspace-card governance-callout"><span>HANDOFF CONTRACT</span><h3>MASTER_VIDEO → Packaging</h3><p>Only an APPROVED master version can be handed to Thumbnail Studio and SEO & Metadata. The handoff carries the exact MasterVersionId, duration, aspect ratio, captions and QA report.</p></section><section className="workspace-card"><div className="card-head"><div><b>Approval requirements</b><small>Before packaging unlocks</small></div></div><ul className="audit-checks"><li>✓ Generation package lineage intact</li><li>✓ Scene ordering locked</li><li>✓ Character continuity passed</li><li>✓ Format / resolution passed</li><li>⚠ Caption QA must be resolved</li><li>⚠ Copyright metadata must be resolved</li><li>○ Human final master approval required</li></ul></section></aside></div></AppShell>}
