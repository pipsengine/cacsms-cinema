'use client';
import {useParams} from 'next/navigation';
import {AppShell} from '@/components/app-shell';import {PublishingTabs} from '@/components/publishing-tabs';import {Status} from '@/components/ui';import {releaseProject,connections} from '@/lib/module12-data';
const tone=(s:string)=>s==='CONNECTED'||s==='HEALTHY'?'green':s==='DEGRADED'?'amber':s==='ERROR'?'red':'gray';
export default function Connections(){const {id}=useParams<{id:string}>();return <AppShell eyebrow={`${releaseProject.code} · RELEASE`} title="Publishing Connections" actions={<button className="btn primary">＋ Connect platform</button>}><PublishingTabs projectId={id} active="connections"/><div className="callout info"><b>Credential security:</b> OAuth tokens/API secrets are stored server-side through credential references and are never exposed to browser code or project manifests.</div><section className="workspace-card"><div className="card-head"><div><b>Platform connections</b><small>Health, scopes and token lifecycle</small></div></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Platform</th><th>Account</th><th>Connection</th><th>Health</th><th>Scopes</th><th>Token expiry</th><th>Actions</th></tr></thead><tbody>{connections.map(c=><tr key={c.platform}><td><b>{c.platform}</b></td><td>{c.account}</td><td><Status tone={tone(c.status) as any}>{c.status.replace('_',' ')}</Status></td><td><Status tone={tone(c.health) as any}>{c.health}</Status></td><td>{c.scopes}</td><td>{c.expires}</td><td><button className="text-btn">{c.status==='CONNECTED'?'Test connection':'Connect'}</button></td></tr>)}</tbody></table></div></section><section className="workspace-card"><div className="card-head"><div><b>Connection policy</b><small>Production rules for external publishing</small></div></div><div className="three-col"><div className="mini-card"><b>Least privilege</b><p>Request only the scopes required for publishing, metadata management and analytics handoff.</p></div><div className="mini-card"><b>Expiry monitoring</b><p>Notify administrators before OAuth credentials expire and block scheduled jobs when health fails.</p></div><div className="mini-card"><b>Revocation safety</b><p>Disconnecting a platform stops future jobs without removing previously published content.</p></div></div></section></AppShell>}
