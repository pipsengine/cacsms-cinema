'use client';
import {useParams} from 'next/navigation';
import {AppShell} from '@/components/app-shell';import {PublishingTabs} from '@/components/publishing-tabs';import {Status} from '@/components/ui';import {releaseProject,publishJobs} from '@/lib/module12-data';
const tone=(s:string)=>s==='SCHEDULED'||s==='PUBLISHED'?'green':s==='RUNNING'?'purple':s==='BLOCKED'||s==='FAILED'?'red':'gray';
export default function Publishing(){const {id}=useParams<{id:string}>();return <AppShell eyebrow={`${releaseProject.code} · RELEASE`} title="Publishing & Scheduling" actions={<><button className="btn secondary">Save schedule</button><button className="btn primary">Approve & schedule</button></>}><PublishingTabs projectId={id} active="publishing"/><div className="callout info"><b>Scheduling rule:</b> a publish job may be drafted before approval, but execution remains locked until the Final Publisher gate is approved.</div><div className="two-col"><section className="workspace-card"><div className="card-head"><div><b>Primary release</b><small>YouTube long form</small></div><Status tone="green">SCHEDULED</Status></div><div className="form-grid"><label>Platform<select className="input"><option>YouTube</option></select></label><label>Connection<select className="input"><option>Cacsms Tv · Healthy</option></select></label><label>Release date<input className="input" value="2026-09-06" readOnly/></label><label>Release time<input className="input" value="18:00" readOnly/></label><label>Timezone<select className="input"><option>Africa/Lagos (WAT)</option></select></label><label>Visibility<select className="input"><option>Public</option><option>Unlisted</option><option>Private</option></select></label><label>Notify subscribers<select className="input"><option>Yes</option><option>No</option></select></label><label>Premiere<select className="input"><option>No</option><option>Yes</option></select></label></div></section><section className="workspace-card"><div className="card-head"><div><b>Execution controls</b><small>Human start/stop remains available</small></div></div><div className="control-stack"><button className="btn primary">▶ Start eligible jobs</button><button className="btn secondary">Ⅱ Pause queue</button><button className="btn secondary">↻ Retry failed jobs</button><button className="btn danger">■ Stop release</button></div><div className="callout warning"><b>Stop behavior:</b> queued jobs are cancelled immediately; already-published platform content is not automatically deleted.</div></section></div><section className="workspace-card"><div className="card-head"><div><b>Platform publishing plan</b><small>One governed job per platform package</small></div></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Job</th><th>Platform</th><th>Title</th><th>Schedule</th><th>Connection</th><th>Attempt</th><th>Status</th></tr></thead><tbody>{publishJobs.map(j=><tr key={j.id}><td><b>{j.id}</b><small>{j.operation}</small></td><td>{j.platform}<small>{j.format}</small></td><td>{j.title}</td><td>{j.scheduled}</td><td>{j.connection}</td><td>{j.attempt}</td><td><Status tone={tone(j.status) as any}>{j.status}</Status></td></tr>)}</tbody></table></div></section></AppShell>}
