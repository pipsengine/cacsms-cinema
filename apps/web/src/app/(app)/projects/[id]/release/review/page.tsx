'use client';
import {useParams} from 'next/navigation';
import {AppShell} from '@/components/app-shell';import {PublishingTabs} from '@/components/publishing-tabs';import {Status} from '@/components/ui';import {releaseProject,approvalSteps,releaseChecks} from '@/lib/module12-data';
const tone=(s:string)=>s==='APPROVED'||s==='PASSED'?'green':s==='PENDING'||s==='NEEDS_REVIEW'?'amber':s==='FAILED'||s==='RETURNED'?'red':'gray';
export default function FinalReview(){const {id}=useParams<{id:string}>();const blockers=releaseChecks.filter(x=>x.blocking&&x.status!=='PASSED');return <AppShell eyebrow={`${releaseProject.code} · RELEASE`} title="Final Review" actions={<><button className="btn secondary">Return for correction</button><button className="btn primary" disabled={blockers.length>0}>Approve final release</button></>}><PublishingTabs projectId={id} active="review"/><div className={blockers.length?'callout warning':'callout info'}><b>{blockers.length?'Approval blocked:':'Approval ready:'}</b> {blockers.length?`${blockers.length} blocking release control(s) require resolution.`:'all blocking checks have passed and the release may be approved.'}</div><div className="three-col"><section className="workspace-card"><div className="card-head"><div><b>Immutable release package</b><small>Exact approved upstream inputs</small></div><Status tone="green">LOCKED</Status></div><div className="detail-list"><div><span>Packaging</span><b>{releaseProject.packagingVersion}</b></div><div><span>Master video</span><b>{releaseProject.master}</b></div><div><span>Primary platform</span><b>YouTube · Long Form</b></div><div><span>Primary thumbnail</span><b>TH-202</b></div><div><span>Caption track</span><b>English · Approved</b></div><div><span>Visibility</span><b>Public</b></div></div></section><section className="workspace-card"><div className="card-head"><div><b>Content preview</b><small>Release-facing metadata</small></div></div><div className="review-preview"><div className="video-placeholder">MASTER v3<br/><small>08:39 · 3840×2160 · 24 fps</small></div><h3>The Hidden AI Already Running Your Everyday Life</h3><p>Artificial intelligence is already shaping what we watch, buy, read and which routes we take — often without us noticing.</p></div></section><section className="workspace-card"><div className="card-head"><div><b>Final publisher decision</b><small>Human-only release gate</small></div></div><div className="approval-box"><label>Decision comment</label><textarea className="text-area" placeholder="Record the reason for approval or return..."/><label>Publish intent</label><select className="input"><option>Approve only</option><option>Approve & schedule</option><option>Approve & publish now</option></select><label>Confirmation</label><div className="check-row"><input type="checkbox"/> I confirm the release package, platform settings and compliance checks.</div></div></section></div><section className="workspace-card"><div className="card-head"><div><b>Approval chain</b><small>Every decision is immutable and audited</small></div></div>{approvalSteps.map(x=><div className="list-row" key={x.id}><div><b>{x.step}. {x.name}</b><small>{x.role} · {x.assignee} · {x.comment}</small></div><Status tone={tone(x.status) as any}>{x.status}</Status></div>)}</section><section className="workspace-card"><div className="card-head"><div><b>Final release controls</b><small>Compliance, integration and scheduling readiness</small></div></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Control</th><th>Category</th><th>Severity</th><th>Evidence</th><th>Status</th></tr></thead><tbody>{releaseChecks.map(x=><tr key={x.id}><td><b>{x.id}</b><small>{x.check}</small></td><td>{x.category}</td><td>{x.severity}</td><td>{x.evidence}</td><td><Status tone={tone(x.status) as any}>{x.status.replace('_',' ')}</Status></td></tr>)}</tbody></table></div></section><section className="workspace-card governance-chain"><div className="card-head"><div><b>Governed release contract</b><small>What happens after final approval</small></div></div><div className="chain-row"><div><span>UPSTREAM</span><b>{releaseProject.packagingVersion}</b><small>Exact PackagingVersionId</small></div><i>→</i><div><span>THIS MODULE</span><b>RELEASE_PACKAGE</b><small>Human-approved publication manifest</small></div><i>→</i><div><span>DOWNSTREAM</span><b>PUBLISH JOBS</b><small>Platform-specific queued operations</small></div></div></section></AppShell>}
