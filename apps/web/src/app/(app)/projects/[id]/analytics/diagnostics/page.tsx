'use client';
import {useParams} from 'next/navigation';
import {AppShell} from '@/components/app-shell';import {AnalyticsTabs} from '@/components/analytics-tabs';import {Status} from '@/components/ui';import {anomalies} from '@/lib/module13-data';
export default function Diagnostics(){const {id}=useParams<{id:string}>();return <AppShell eyebrow="PERFORMANCE INTELLIGENCE" title="Experiments & Alerts" actions={<button className="btn primary">Create experiment</button>}><AnalyticsTabs projectId={id}/><section className="workspace-card"><div className="card-head"><div><b>Anomaly detection</b><small>Unexpected changes compared with channel/platform baselines</small></div></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Alert</th><th>Metric</th><th>Expected</th><th>Actual</th><th>Severity</th><th>Status</th></tr></thead><tbody>{anomalies.map(a=><tr key={a.id}><td><b>{a.title}</b><small>{a.detail}</small></td><td>{a.metric}</td><td>{a.expected}</td><td>{a.actual}</td><td><Status tone={a.severity==='MEDIUM'?'amber':'blue'}>{a.severity}</Status></td><td><Status tone="amber">{a.status}</Status></td></tr>)}</tbody></table></div></section><section className="workspace-card"><div className="card-head"><div><b>Controlled experiments</b><small>Title/thumbnail tests must preserve the approved factual release package</small></div></div><div className="empty-intel"><b>No active experiment</b><p>Create an A/B packaging experiment after enough traffic is available. Experiment variants remain linked to this PublicationId and cannot modify the approved master video.</p><button className="btn secondary">Create title / thumbnail test</button></div></section></AppShell>}
