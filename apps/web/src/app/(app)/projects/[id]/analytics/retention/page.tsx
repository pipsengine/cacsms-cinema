'use client';
import {useParams} from 'next/navigation';
import {AppShell} from '@/components/app-shell';import {AnalyticsTabs} from '@/components/analytics-tabs';import {Status} from '@/components/ui';import {retention} from '@/lib/module13-data';
export default function Retention(){const {id}=useParams<{id:string}>();return <AppShell eyebrow="PERFORMANCE INTELLIGENCE" title="Audience Retention"><AnalyticsTabs projectId={id}/><div className="two-col analytics-retention"><section className="workspace-card"><div className="card-head"><div><b>Retention curve</b><small>Actual viewer retention mapped against production markers</small></div><Status tone="green">69.8% AVG VIEWED</Status></div><div className="retention-chart">{retention.map(r=><div key={r.second} className="ret-point" style={{left:`${r.second/410*94+2}%`,bottom:`${r.pct*.72}%`}}><i/><span>{r.pct}%</span></div>)}<div className="ret-line"/></div><div className="ret-axis">{retention.map(r=><span key={r.second}>{Math.floor(r.second/60)}:{String(r.second%60).padStart(2,'0')}</span>)}</div></section><section className="workspace-card"><div className="card-head"><div><b>Retention diagnosis</b><small>Explainable observations, not hidden scoring</small></div></div>{[['0:00–0:30','82% retained','Strong','Opening question works, but 18% early loss remains the largest single drop.'],['0:30–2:00','70% retained','Strong','Context stays concise; pattern reveal prevents a typical second-minute dip.'],['2:00–5:00','61% retained','Strong','Story structure remains stable through midpoint and human-choice section.'],['5:00–6:50','54% retained','Healthy','Closing decline is gradual rather than abrupt; CTA does not trigger a cliff.']].map(x=><div className="diagnostic-row" key={x[0]}><div><b>{x[0]}</b><small>{x[3]}</small></div><div><strong>{x[1]}</strong><Status tone="green">{x[2]}</Status></div></div>)}</section></div></AppShell>}
