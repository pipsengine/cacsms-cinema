'use client';
import {useParams} from 'next/navigation';
import {AppShell} from '@/components/app-shell';import {AnalyticsTabs} from '@/components/analytics-tabs';import {StatCard,Status} from '@/components/ui';import {analyticsProject,snapshots,traffic} from '@/lib/module13-data';
export default function Performance(){const {id}=useParams<{id:string}>();const x=snapshots.at(-1)!;return <AppShell eyebrow="PERFORMANCE INTELLIGENCE" title="Performance Intelligence" actions={<><button className="btn secondary">Refresh platform metrics</button><button className="btn primary">Run AI diagnosis</button></>}><AnalyticsTabs projectId={id}/><div className="lineage-strip"><span>Publication</span><b>{analyticsProject.publicationId}</b><i>→</i><span>Platform</span><b>YouTube</b><i>→</i><span>Window</span><b>48 Hours</b><i>→</i><Status tone="purple">MONITORING</Status></div><div className="stat-grid"><StatCard label="Views" value="90,600" detail="+38% vs 48h benchmark" icon="▶"/><StatCard label="CTR" value="9.2%" detail="Channel median 7.1%" icon="↗"/><StatCard label="Watch time" value="7,198h" detail="Avg 4:46 per view" icon="◫"/><StatCard label="Subscribers" value="+2,540" detail="2.80% view-to-sub rate" icon="＋"/></div><div className="analytics-layout"><section className="workspace-card"><div className="card-head"><div><b>Performance growth · first 48 hours</b><small>Time-series snapshots are immutable ingestion records</small></div></div><div className="trend-chart">{snapshots.map((s,i)=><div key={s.window} className="trend-col"><span style={{height:`${22+i*21}%`}}/><b>{(s.views/1000).toFixed(1)}k</b><small>{s.window}</small></div>)}</div></section><section className="workspace-card"><div className="card-head"><div><b>Acquisition</b><small>Where viewers found the publication</small></div></div>{traffic.map(t=><div className="bar-row" key={t[0]}><span>{t[0]}</span><div><i style={{width:t[1]}}/></div><b>{t[1]}</b></div>)}</section></div><section className="workspace-card"><div className="card-head"><div><b>48-hour metric table</b><small>Platform snapshots for benchmark and anomaly analysis</small></div></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Window</th><th>Impressions</th><th>Views</th><th>CTR</th><th>Avg. duration</th><th>Avg. viewed</th><th>Subs</th><th>Revenue</th></tr></thead><tbody>{snapshots.map(s=><tr key={s.window}><td><b>{s.window}</b></td><td>{s.impressions.toLocaleString()}</td><td>{s.views.toLocaleString()}</td><td>{s.ctr}%</td><td>{Math.floor(s.avd/60)}:{String(s.avd%60).padStart(2,'0')}</td><td>{s.apv}%</td><td>+{s.subs.toLocaleString()}</td><td>${s.revenue.toFixed(2)}</td></tr>)}</tbody></table></div></section></AppShell>}
