'use client';
import {useParams} from 'next/navigation';
import {AppShell} from '@/components/app-shell';import {AnalyticsTabs} from '@/components/analytics-tabs';import {Status} from '@/components/ui';import {insights} from '@/lib/module13-data';
export default function Learning(){const {id}=useParams<{id:string}>();return <AppShell eyebrow="AI LEARNING" title="AI Learning Review"><AnalyticsTabs projectId={id}/><div className="callout warning"><b>Human learning gate:</b> AI may propose a lesson, but it cannot change channel strategy, scoring weights or future prompts until a reviewer approves the insight.</div><div className="learning-grid">{insights.map(i=><article className="learning-card" key={i.id}><div className="learning-head"><div><span>{i.category}</span><b>{i.type}</b></div><Status tone="amber">PROPOSED</Status></div><h3>{i.title}</h3><p>{i.text}</p><div className="learning-scores"><span>Confidence <b>{i.confidence}</b></span><span>Impact <b>{i.impact}</b></span></div><div className="control-stack"><button className="btn secondary">Reject</button><button className="btn primary">Approve lesson</button></div></article>)}</div></AppShell>}
