'use client';import {useParams} from 'next/navigation';import {AppShell} from '@/components/app-shell';import {PackagingTabs} from '@/components/packaging-tabs';import {Status} from '@/components/ui';import {packagingProject,chapters,captionTracks} from '@/lib/module11-data';
const tone=(s:string)=>s==='APPROVED'||s==='READY'?'green':s==='NOT_STARTED'?'gray':'amber';
export default function Captions(){const {id}=useParams<{id:string}>();return <AppShell eyebrow={`${packagingProject.code} · PACKAGING`} title="Captions & Chapters" actions={<><button className="btn secondary">Validate timecodes</button><button className="btn primary">Export SRT / VTT</button></>}><PackagingTabs projectId={id as string} active="captions"/><div className="two-col"><section className="workspace-card"><div className="card-head"><div><b>Caption tracks</b><small>Accessibility and platform subtitle assets</small></div><Status tone="green">ENGLISH APPROVED</Status></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Track</th><th>Language</th><th>Type</th><th>Coverage</th><th>Source</th><th>Format</th><th>Status</th></tr></thead><tbody>{captionTracks.map(c=><tr key={c.id}><td><b>{c.id}</b></td><td>{c.language}</td><td>{c.kind.replaceAll('_',' ')}</td><td>{c.coverage}%</td><td>{c.source}</td><td>{c.format}</td><td><Status tone={tone(c.status) as any}>{c.status.replace('_',' ')}</Status></td></tr>)}</tbody></table></div></section><section className="workspace-card"><div className="card-head"><div><b>Chapter validation</b><small>{chapters.length} sections · {packagingProject.duration} master</small></div><Status tone="green">VALIDATED</Status></div>{chapters.map((c,i)=><div className="chapter-row" key={c.time}><b>{c.time}</b><span>{c.title}</span><small>SC-{String(i+1).padStart(3,'0')}</small><Status tone="green">READY</Status></div>)}</section></div><section className="workspace-card"><div className="card-head"><div><b>Accessibility packaging</b><small>Required downstream assets</small></div></div><div className="three-col compact"><div className="mini-panel"><span>Closed captions</span><b>100% timed</b><small>SRT + VTT</small></div><div className="mini-panel"><span>Clean transcript</span><b>Approved</b><small>TXT master</small></div><div className="mini-panel"><span>Translated captions</span><b>Optional</b><small>FR / ES queued only when approved</small></div></div></section></AppShell>}
