import {AppShell} from '@/components/app-shell';import {AdminTabs} from '@/components/admin-tabs';import {Status} from '@/components/ui';import {healthServices} from '@/lib/module14-data';
export default function Page(){return <AppShell eyebrow="OPERATIONS" title="System Health & Monitoring" actions={<button className="btn secondary">Run health checks</button>}><AdminTabs/><section className="workspace-card"><div className="card-head"><div><b>Service health</b><small>Application, data, workers and external dependencies</small></div><Status tone="green">SYSTEM OPERATIONAL</Status></div><table className="enterprise-table"><thead><tr><th>Service</th><th>Status</th><th>Latency</th><th>Uptime</th><th>Last check</th></tr></thead><tbody>{healthServices.map(s=><tr key={s.name}><td><b>{s.name}</b></td><td><Status tone={s.status==='DEGRADED'?'amber':'green'}>{s.status}</Status></td><td>{s.latency}</td><td>{s.uptime}</td><td>{s.last}</td></tr>)}</tbody></table></section></AppShell>}
