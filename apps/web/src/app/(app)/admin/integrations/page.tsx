import {AppShell} from '@/components/app-shell';import {AdminTabs} from '@/components/admin-tabs';import {Status} from '@/components/ui';import {integrations} from '@/lib/module14-data';
export default function Page(){return <AppShell eyebrow="PLATFORM INTEGRATIONS" title="AI & Social Integrations" actions={<button className="btn primary">＋ Add Integration</button>}><AdminTabs/><section className="workspace-card"><div className="card-head"><div><b>Provider registry</b><small>Credentials stay in server-side secret storage</small></div></div><div className="integration-grid">{integrations.map(i=><article className="integration-card" key={i.name}><div className="integration-icon">{i.group==='AI'?'✦':'↗'}</div><div className="integration-main"><div><span>{i.group}</span><h3>{i.name}</h3></div><p>{i.capability}</p><div className="detail-list"><div><span>Authentication</span><b>{i.auth}</b></div><div><span>Last health check</span><b>{i.last}</b></div></div></div><div className="integration-actions"><Status tone={i.health==='DEGRADED'?'amber':i.status==='NOT_CONNECTED'?'gray':'green'}>{i.status}</Status><button className="btn secondary small">Configure</button><button className="btn secondary small">Test</button></div></article>)}</div></section></AppShell>}
