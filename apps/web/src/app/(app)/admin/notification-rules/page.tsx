import {AppShell} from '@/components/app-shell';import {AdminTabs} from '@/components/admin-tabs';import {Status} from '@/components/ui';import {notificationRules} from '@/lib/module14-data';
export default function Page(){return <AppShell eyebrow="OPERATIONS" title="Notification Administration" actions={<button className="btn primary">＋ New Rule</button>}><AdminTabs/><section className="workspace-card"><div className="card-head"><div><b>Notification rules</b><small>Severity, channels, recipients and escalation policy</small></div></div><table className="enterprise-table"><thead><tr><th>Rule</th><th>Channel</th><th>Severity</th><th>Recipients</th><th>State</th><th/></tr></thead><tbody>{notificationRules.map(r=><tr key={r.name}><td><b>{r.name}</b></td><td>{r.channel}</td><td><Status tone={r.severity==='CRITICAL'?'red':r.severity==='WARNING'?'amber':'blue'}>{r.severity}</Status></td><td>{r.recipients}</td><td><Status tone={r.enabled?'green':'gray'}>{r.enabled?'ENABLED':'DISABLED'}</Status></td><td><button className="btn secondary small">Edit</button></td></tr>)}</tbody></table></section></AppShell>}
