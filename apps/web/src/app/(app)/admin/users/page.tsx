'use client'; import {useState} from 'react'; import {AppShell} from '@/components/app-shell'; import {StatCard,Status} from '@/components/ui'; import {users} from '@/lib/module01-data';
const tone=(s:string)=>s==='Active'?'green':s==='Invited'?'blue':'red';
export default function Users(){const [query,setQuery]=useState(''); const [open,setOpen]=useState(false); const list=users.filter(u=>(u.name+u.email+u.role).toLowerCase().includes(query.toLowerCase())); return <AppShell title="Users" actions={<><button className="btn secondary">Export</button><button className="btn primary" onClick={()=>setOpen(true)}>＋ Invite user</button></>}><p className="page-subtitle">Manage workspace members, access status, roles and security posture.</p><div className="stats-grid four"><StatCard icon="♙" label="Total users" value="8" detail="Across this workspace"/><StatCard icon="✓" label="Active" value="6" detail="75% of users"/><StatCard icon="✉" label="Invited" value="1" detail="Awaiting activation"/><StatCard icon="✦" label="MFA enabled" value="5" detail="63% coverage"/></div><section className="table-card"><div className="table-toolbar"><div className="search-input">⌕<input placeholder="Search users, email or role…" value={query} onChange={e=>setQuery(e.target.value)}/></div><button className="filter-btn">Status: All ⌄</button><button className="filter-btn">Role: All ⌄</button><span className="table-count">{list.length} users</span></div><div className="table-wrap"><table><thead><tr><th><input type="checkbox"/></th><th>User</th><th>Role</th><th>Status</th><th>MFA</th><th>Last activity</th><th/></tr></thead><tbody>{list.map(u=><tr key={u.email}><td><input type="checkbox"/></td><td><div className="user-cell"><div className="avatar">{u.initials}</div><div><b>{u.name}</b><span>{u.email}</span></div></div></td><td><span className="role-badge">{u.role}</span></td><td><Status tone={tone(u.status) as any}>{u.status}</Status></td><td>{u.mfa?<span className="mfa yes">✓ Enabled</span>:<span className="mfa no">— Not enabled</span>}</td><td>{u.last}</td><td><button className="dots">•••</button></td></tr>)}</tbody></table></div><div className="table-footer"><span>Showing 1–{list.length} of {list.length}</span><div><button disabled>←</button><button className="page-active">1</button><button disabled>→</button></div></div></section>{open&&<div className="modal-backdrop" onClick={()=>setOpen(false)}><div className="drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><div><div className="eyebrow">NEW MEMBER</div><h2>Invite user</h2><p>Invite a person and define their initial workspace access.</p></div><button className="close" onClick={()=>setOpen(false)}>×</button></div><div className="drawer-body"><label>Email address<input placeholder="name@company.com"/></label><label>Display name<input placeholder="Full name"/></label><label>Workspace role<select><option>Creator</option><option>Reviewer</option><option>Content Manager</option><option>Viewer</option></select></label><label className="toggle-row"><div><b>Require MFA</b><span>User must configure MFA during activation.</span></div><input type="checkbox" defaultChecked/></label><div className="notice blue">ℹ The user will receive a time-limited activation invitation.</div></div><div className="drawer-foot"><button className="btn secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="btn primary">Send invitation</button></div></div></div>}</AppShell>}
