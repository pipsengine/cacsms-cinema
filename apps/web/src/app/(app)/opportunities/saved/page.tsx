'use client';
import Link from 'next/link';import {AppShell} from '@/components/app-shell';import {Status} from '@/components/ui';import {opportunities} from '@/lib/module05-data';
export default function Saved(){const saved=opportunities.filter(x=>x.status==='SHORTLISTED'||x.status==='SELECTED');return <AppShell eyebrow="OPPORTUNITY INTELLIGENCE" title="Saved & Shortlisted Opportunities" actions={<Link href="/opportunities" className="btn primary">Discover more</Link>}><p className="page-subtitle">A governed watchlist of high-potential ideas retained for selection, future projects or alternate packaging.</p><section className="workspace-card saved-opp"><div className="saved-head"><span>Opportunity</span><span>Project</span><span>Score</span><span>Global</span><span>Viral</span><span>Status</span><span>Action</span></div>{saved.map(x=><div className="saved-row" key={x.id}><div><b>{x.title}</b><small>{x.topic} · {x.format}</small></div><span>{x.projectCode}</span><strong>{x.score}</strong><span>{x.global}</span><span>{x.viral}</span><Status tone={x.status==='SELECTED'?'green':'blue'}>{x.status}</Status><Link href={`/opportunities/${x.id}`}>Open →</Link></div>)}</section></AppShell>}
