import Link from 'next/link';import {AppShell} from '@/components/app-shell';import {StatCard,Status} from '@/components/ui';import {releaseProject,approvalSteps,releaseChecks,publishJobs} from '@/lib/module12-data';
const tone=(s:string)=>s==='APPROVED'||s==='PASSED'||s==='CONNECTED'||s==='HEALTHY'?'green':s==='PENDING'||s==='NEEDS_REVIEW'||s==='SCHEDULED'?'amber':s==='BLOCKED'||s==='FAILED'||s==='RETURNED'?'red':s==='IN_PROGRESS'?'purple':'gray';
export default function Approval(){const blocking=releaseChecks.filter(x=>x.blocking&&x.status!=='PASSED').length;return <AppShell eyebrow="APPROVAL & RELEASE" title="Approval Center" actions={<><button className="btn secondary">Export release manifest</button><Link className="btn primary" href={`/projects/${releaseProject.id}/release/review`}>Open final review</Link></>}><div className="callout info"><b>Governed input:</b> {releaseProject.packagingVersion} is locked to this release. Final approval cannot silently adopt later packaging edits.</div><div className="stat-grid"><StatCard label="Approval progress" value={releaseProject.approvalStep} detail="Human approval steps" icon="✓"/><StatCard label="Blocking controls" value={blocking} detail="Must reach zero to publish" icon="!"/><StatCard label="Scheduled jobs" value={publishJobs.filter(x=>x.status==='SCHEDULED').length} detail="Pending final approval" icon="□"/><StatCard label="Release readiness" value="72%" detail="Final publisher gate open" icon="↗"/></div><section className="workspace-card"><div className="card-head"><div><b>Release pipeline</b><small>{releaseProject.code} · {releaseProject.title}</small></div><Status tone="amber">AWAITING APPROVAL</Status></div><div className="post-flow"><div className="done"><b>01</b><span>Packaging package</span><small>{releaseProject.packagingVersion}</small></div><i>→</i><div className="done"><b>02</b><span>Content review</span><small>Approved</small></div><i>→</i><div className="active"><b>03</b><span>Final publisher</span><small>Pending</small></div><i>→</i><div><b>04</b><span>Schedule / publish</span><small>Controlled</small></div><i>→</i><div><b>05</b><span>Performance handoff</span><small>Locked</small></div></div></section><div className="two-col"><section className="workspace-card"><div className="card-head"><div><b>Approval chain</b><small>Human gates cannot be bypassed by agents</small></div></div>{approvalSteps.map(x=><div className="list-row" key={x.id}><div><b>{x.step}. {x.name}</b><small>{x.role} · {x.assignee}{x.decided?` · ${x.decided}`:''}</small></div><Status tone={tone(x.status) as any}>{x.status}</Status></div>)}</section><section className="workspace-card"><div className="card-head"><div><b>Release blockers</b><small>Only unresolved blocking controls shown</small></div></div>{releaseChecks.filter(x=>x.blocking&&x.status!=='PASSED').map(x=><div className="list-row" key={x.id}><div><b>{x.id} · {x.check}</b><small>{x.evidence}</small></div><Status tone={tone(x.status) as any}>{x.status.replace('_',' ')}</Status></div>)}</section></div><section className="workspace-card"><div className="card-head"><div><b>Upcoming publication jobs</b><small>Platform queue created from approved platform packages</small></div><Link href={`/projects/${releaseProject.id}/release/jobs`}>Open queue →</Link></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Job</th><th>Platform</th><th>Schedule</th><th>Visibility</th><th>Connection</th><th>Status</th></tr></thead><tbody>{publishJobs.map(j=><tr key={j.id}><td><b>{j.id}</b><small>{j.format}</small></td><td>{j.platform}</td><td>{j.scheduled}</td><td>{j.visibility}</td><td>{j.connection}</td><td><Status tone={tone(j.status) as any}>{j.status}</Status></td></tr>)}</tbody></table></div></section></AppShell>}
