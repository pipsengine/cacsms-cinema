'use client'; import {workspaces} from '@/lib/module01-data';
export default function Workspace(){return <main className="workspace-page"><div className="workspace-header"><div className="auth-brand"><div className="brand-mark">CC</div><div><strong>Cacsms Cinema</strong><span>Content Operations OS</span></div></div><div className="avatar">PE</div></div><section className="workspace-content"><div className="eyebrow">WORKSPACE ACCESS</div><h1>Choose your workspace</h1><p>Select the operating environment you want to enter. Your role and permissions may differ by workspace.</p><div className="workspace-list">{workspaces.map((w,i)=><button className="workspace-card" key={w.id} onClick={()=>location.href='/profile'}><div className={`workspace-logo c${i}`}>{w.code}</div><div className="workspace-info"><div><h2>{w.name}</h2><span>{w.plan}</span></div><p>{w.role} · {w.members} members · {w.projects} content projects</p><small>Last opened {w.last}</small></div><span className="workspace-arrow">→</span></button>)}</div><button className="workspace-create">＋ Create another workspace</button></section><footer className="workspace-footer">Signed in as <b>pipsengine@gmail.com</b> · <a href="/login">Sign out</a></footer></main>}
