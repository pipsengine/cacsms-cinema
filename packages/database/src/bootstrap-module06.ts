import {closeDb,getDb,sql} from './index.js';
const email=process.env.BOOTSTRAP_ADMIN_EMAIL||'pipsengine@gmail.com';
try{const db=await getDb();const base=(await db.request().input('email',sql.NVarChar(320),email).query(`SELECT TOP 1 u.UserId,w.WorkspaceId FROM dbo.Users u CROSS JOIN dbo.Workspaces w WHERE u.Email=@email AND w.Slug='cacsms-cinemas'`)).recordset[0];if(!base)throw new Error('Run Module 01 bootstrap first.');const opp=(await db.request().input('ws',sql.UniqueIdentifier,base.WorkspaceId).query(`SELECT TOP 1 o.ContentOpportunityId,o.ContentProjectId FROM dbo.ContentOpportunities o JOIN dbo.ContentProjects p ON p.ContentProjectId=o.ContentProjectId WHERE p.WorkspaceId=@ws AND o.Status='SELECTED' ORDER BY o.SelectedAt DESC`)).recordset[0];if(!opp)throw new Error('Run Module 05 bootstrap first.');
 await db.request().input('pid',sql.UniqueIdentifier,opp.ContentProjectId).input('oid',sql.UniqueIdentifier,opp.ContentOpportunityId).input('uid',sql.UniqueIdentifier,base.UserId).query(`IF NOT EXISTS(SELECT 1 FROM dbo.ResearchPacks WHERE ContentProjectId=@pid) INSERT dbo.ResearchPacks(ContentProjectId,SourceOpportunityId,Status,CurrentVersionNumber,ResearchObjective,ResearchSummary,ExecutiveFindings,NarrativeAngles,ProductionNotes,SensitivitySummary,ResearchProvider,ResearchModel,StartedAt,CreatedByUserId) VALUES(@pid,@oid,'IN_REVIEW',1,'Establish authoritative evidence for how invisible AI systems influence everyday consumer experiences.','Research establishes recommendation systems, fraud detection, ranking and personalization as practical examples while separating verified capabilities from speculation.','Use concrete consumer-facing examples; distinguish AI from simple automation; avoid deterministic claims about individual decisions.','Open with a normal morning routine, reveal the hidden systems behind each interaction, then explain limits and trade-offs.','Prioritize visual examples that can be recreated without showing private data or misleading interfaces.','Privacy and algorithmic fairness claims require careful sourcing and wording.','Research Agent Subscription','Research Intelligence v1',DATEADD(hour,-2,SYSUTCDATETIME()),@uid)`);
 const pack=(await db.request().input('pid',sql.UniqueIdentifier,opp.ContentProjectId).query(`SELECT ResearchPackId FROM dbo.ResearchPacks WHERE ContentProjectId=@pid`)).recordset[0];
 const qs=[['How do recommendation systems rank content for users?','HIGH','ANSWERED','They combine behavioural and contextual signals to estimate relevance; exact methods vary by platform.'],['Where is AI used in fraud detection and transaction risk?','HIGH','ANSWERED','Financial institutions use statistical and machine-learning models to flag patterns associated with fraud risk.'],['Which claims about personalized pricing are defensible?','HIGH','OPEN','Requires market-specific evidence and careful distinction between dynamic pricing, segmentation and individualized pricing.'],['What limitations should the documentary state explicitly?','MEDIUM','ANSWERED','Automated decisions vary by system; not every automated rule is AI; outputs are probabilistic and can be wrong.']];
 for(let i=0;i<qs.length;i++)await db.request().input('rid',sql.UniqueIdentifier,pack.ResearchPackId).input('q',sql.NVarChar(1500),qs[i][0]).input('priority',sql.NVarChar(20),qs[i][1]).input('status',sql.NVarChar(30),qs[i][2]).input('answer',sql.NVarChar(sql.MAX),qs[i][3]).input('sort',sql.Int,i+1).query(`IF NOT EXISTS(SELECT 1 FROM dbo.ResearchQuestions WHERE ResearchPackId=@rid AND QuestionText=@q) INSERT dbo.ResearchQuestions(ResearchPackId,QuestionText,Priority,Status,AnswerSummary,SortOrder) VALUES(@rid,@q,@priority,@status,@answer,@sort)`);
 const sources=[['PRIMARY','National Institute of Standards and Technology','Artificial Intelligence Risk Management Framework','https://www.nist.gov/itl/ai-risk-management-framework','HIGH',96,86,93,1],['PRIMARY','Google','How YouTube recommendations work','https://support.google.com/youtube/','HIGH',88,83,96,1],['PRIMARY','Meta','How AI influences what you see on Facebook and Instagram','https://transparency.meta.com/','HIGH',90,82,94,1],['SECONDARY','OECD','AI principles and responsible AI resources','https://oecd.ai/','HIGH',92,88,84,0],['SECONDARY','Academic / industry literature','Machine learning approaches to fraud detection','', 'MEDIUM',82,78,89,0]];
 for(const s of sources)await db.request().input('rid',sql.UniqueIdentifier,pack.ResearchPackId).input('type',sql.NVarChar(60),s[0]).input('pub',sql.NVarChar(200),s[1]).input('title',sql.NVarChar(500),s[2]).input('url',sql.NVarChar(1500),s[3]||null).input('quality',sql.NVarChar(30),s[4]).input('auth',sql.Decimal(5,2),s[5] as number).input('rec',sql.Decimal(5,2),s[6] as number).input('rel',sql.Decimal(5,2),s[7] as number).input('primary',sql.Bit,s[8] as number).query(`IF NOT EXISTS(SELECT 1 FROM dbo.ResearchSources WHERE ResearchPackId=@rid AND Title=@title) INSERT dbo.ResearchSources(ResearchPackId,SourceType,Publisher,Title,Url,AuthorityScore,RecencyScore,RelevanceScore,SourceQuality,IsPrimarySource,IsApproved,AccessedAt,CitationLabel) VALUES(@rid,@type,@pub,@title,@url,@auth,@rec,@rel,@quality,@primary,1,SYSUTCDATETIME(),@pub)`);
 const claims=[['CLM-001','Recommendation systems rank and recommend content using signals about likely relevance rather than presenting all available content equally.','VERIFIED',96,'HIGH','APPROVED'],['CLM-002','Machine-learning models are widely used as part of fraud-detection systems to identify suspicious transaction patterns.','VERIFIED',94,'HIGH','APPROVED'],['CLM-003','A consumer may receive a different price solely because an AI system predicted their willingness to pay.','NEEDS_REVIEW',54,'HIGH','HOLD'],['CLM-004','Not every automated digital decision should be described as artificial intelligence.','VERIFIED',98,'HIGH','APPROVED'],['CLM-005','Recommendation outputs are probabilistic and may reflect imperfect signals or biases.','VERIFIED',91,'MEDIUM','APPROVED']];
 for(const c of claims)await db.request().input('rid',sql.UniqueIdentifier,pack.ResearchPackId).input('code',sql.NVarChar(40),c[0]).input('text',sql.NVarChar(sql.MAX),c[1]).input('status',sql.NVarChar(30),c[2]).input('conf',sql.Decimal(5,2),c[3] as number).input('mat',sql.NVarChar(20),c[4]).input('usage',sql.NVarChar(30),c[5]).query(`IF NOT EXISTS(SELECT 1 FROM dbo.ResearchClaims WHERE ResearchPackId=@rid AND ClaimCode=@code) INSERT dbo.ResearchClaims(ResearchPackId,ClaimCode,ClaimText,VerificationStatus,ConfidenceScore,Materiality,ScriptUsageStatus,VerificationNotes) VALUES(@rid,@code,@text,@status,@conf,@mat,@usage,'Seeded verification state for Module 06.')`);
 await db.request().input('rid',sql.UniqueIdentifier,pack.ResearchPackId).query(`IF NOT EXISTS(SELECT 1 FROM dbo.ResearchContradictions WHERE ResearchPackId=@rid) INSERT dbo.ResearchContradictions(ResearchPackId,Subject,PositionA,PositionB,ResolutionStatus,ResolutionNotes) VALUES(@rid,'Personalized pricing','Some reporting uses personalized pricing broadly.','Primary evidence often supports dynamic or segmented pricing rather than one-to-one willingness-to-pay predictions.','OPEN','Use conservative wording until a specific authoritative example is verified.'); IF NOT EXISTS(SELECT 1 FROM dbo.ResearchRisks WHERE ResearchPackId=@rid) INSERT dbo.ResearchRisks(ResearchPackId,RiskCategory,Severity,Description,Mitigation,Status) VALUES(@rid,'Accuracy','HIGH','Overstating what an algorithm knows about an individual.','Use probabilistic language and approved claims only.','OPEN'),(@rid,'Privacy','MEDIUM','Visuals could imply access to private personal data.','Use fictionalized interfaces and avoid real personal information.','MITIGATED');`);
 console.log('Module 06 research and fact intelligence bootstrap complete.');}finally{await closeDb()}
