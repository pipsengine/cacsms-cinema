{
  "name": "cacsms-cinema",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "set NEXT_PUBLIC_DEMO_MODE=true && pnpm --parallel --filter @cacsms/api --filter @cacsms/web dev",
    "start": "pnpm dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22"
  }
}
